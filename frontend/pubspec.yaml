name: frontend
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  shared_models:
    path: ../shared_models

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
